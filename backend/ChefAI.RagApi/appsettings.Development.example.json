{
  "_comment": "Secrets go in .env (gitignored). Copy .env.example → .env and fill in values. NEVER put real keys here.",
  "RagDb": {
    "Password": "REPLACE_ME"
  },
  "OpenRouter": {
    "ApiKey": "REPLACE_ME"
  }
}
